# S1-04 冻结实施计划

## 1. 冻结元数据

| 字段 | 值 |
|---|---|
| issue_id | S1-04 |
| plan_version | S1-04-plan-v1 |
| frozen | true |
| solution_version | solution-v1 |
| design_version | S1-04-design-v1 |
| max_luna_wave_size | 1 |

## 2. 冻结执行单元

| unit_id | executor | wave | depends_on | owned_files | objective | validation |
|---|---|---|---|---|---|---|
| S00 | sol | serial | - | docs/01_Sprint记录/Sprint01/S1-04/S1-04_方案实施总结.md;docs/01_Sprint记录/Sprint01/S1-04/S1-04_实施记录.md | 核验已发布设计、implementation input-gate、固定 S1-04 target 与首次 Sprint→Issue 同步；绑定既有 base_python3.12 解释器并建立可恢复阶段记录 | anchor status=ok；branch/worktree/HEAD/plan version 唯一；implementation_start 只记录一次；解释器 probe 与当前 worktree 源码定位通过 |
| P01 | luna | 1 | S00 | src/config_manager/config_node.py;src/config_manager/core/manager.py;src/config_manager/core/watcher.py;src/config_manager/config_manager.py;tests/01_unit_tests/test_config_manager/test_progress_autosave.py | 先写冻结 focused tests 并记录 red，再实现 keyed autosave 批次、progress-only 无备份保存、watcher RLock/watermark 确认和 facade 单次调度；交付一个 green package commit | package 只含五个 owned files；focused red 只指向缺失批准行为；green focused 全过；普通/mixed/unknown/explicit/external/failure/re-entry 边界均有确定性断言 |
| S01 | sol | serial | P01 | docs/01_Sprint记录/Sprint01/S1-04/S1-04_方案实施总结.md;docs/01_Sprint记录/Sprint01/S1-04/S1-04_实施记录.md;docs/01_Sprint记录/Sprint01/S1-04/S1-04_测试报告.md | 核验并集成 P01，重建实际影响面，执行 focused/affected/full、Python quality、code-craft 与设计符合性；完成 stage check、Issue 成果提交、accepted manifest、Sprint merge、待验收与 acceptance input-gate | P01 commit 是 Issue HEAD 祖先；七节测试报告完整；plan/quality/conformance/issue/affected/full/Git 门禁有当前退出码；stage_check=pass或逐项 legacy；Sprint 状态=待验收且 manifest verified |

## 3. 单元资格与 owner 证据

| unit | simple | clear | bounded | package_safety | owner 结论 |
|---|---|---|---|---|---|
| S00 | fail：target、首次同步、runtime 和阶段状态归 owner | pass | pass | fail：不得由 package 创建或同步 Issue target | `executor=sol` |
| P01 | pass：设计已冻结算法、状态、失败语义与测试 seam | pass | pass：五个互斥文件，一个行为闭环 | pass：独立 package target，不写阶段/Sprint 状态 | `executor=luna`；singleton wave 1 |
| S01 | fail：package 集成、完整影响、门禁、manifest、merge 与收尾归 owner | pass | pass | fail：跨 package/Issue/Sprint 终态权 | `executor=sol` |

P01 的 planned executor 固定为 Luna。只有 Luna 创建在零写入状态明确不可用且 P01 尚未开始时，Terra 才接管同一信封并记录 `planned_executor=luna / actual_executor=terra`；Sol 禁止接管。实施首评整改不再派发 Luna/Terra。

## 4. 冻结 target 与 DAG

```yaml
stage_target:
  stage: implementation
  target_kind: issue_worktree
  branch: S1-04
  worktree_path: D:\Tony\Documents\invest2025\project\config_manager\.worktrees\S1-04
  target_id: S1-04
  base_commit: <首次 implementation_start 同步后的 Sprint HEAD>
```

设计成果成为 Sprint HEAD 祖先前禁止创建该 target。首次实施由锚点脚本返回 `state=existing|create` 后核验或创建；恢复命中有效 `implementation_start` 后禁止再次同步、刷新基线或换 target。

```text
S00(sol: input/target/runtime/start)
  -> P01(luna wave1: red→green package)
  -> S01(sol: integrate/gates/commit/merge/closure)
```

## 5. TDD、验证与隔离

P01 在同一 package 工作区先只写 focused tests，使用冻结命令观察 red；确认失败原因是旧实现缺少 keyed intent、progress-only routing 或 watcher atomic confirmation 后，再修改生产代码至 green。一个 package 只提交最终 green commit；red 命令、node 和退出码写入实施记录。

S01 至少执行并记录：

1. `D:\anaconda3\envs\base_python3.12\python.exe C:\Users\Tony\.codex\skills\design-plan\scripts\validate_implementation_plan.py docs\01_Sprint记录\Sprint01\S1-04\S1-04_实施计划.md --max-luna-capacity 1`；
2. focused：`tests/01_unit_tests/test_config_manager/test_progress_autosave.py`；
3. affected：autosave、internal-save、frequent-save、backup、YAML codec 与 sequence persistence 六个冻结文件；
4. full：`D:\anaconda3\envs\base_python3.12\python.exe -m pytest tests -v`；
5. 实际 Python changed paths 的 code standards、code-craft apply/review 与 design conformance；
6. Git：P01 commit/branch tip 为 Issue HEAD 祖先，最终 manifest 为 Sprint HEAD 祖先，零未合并路径。

每例只使用临时配置和当前测试实例，显式 cleanup timer/watcher。同步测试使用 Event/Barrier，不用 sleep 或输出字符串单独证明并发。`database_test_isolation_result=not_applicable_zero_connection_scope`。

## 6. Dispatch、集成与恢复

P01 spawn 前，Sol 在 `S1-04_实施记录.md` 写入：

```text
action_key=Sprint01:S1-04:implementation:P01
stable_task_name=sprint01_s1_04_p01
task_mode=package
plan_version=S1-04-plan-v1
target=work/S1-04/P01@.worktrees/S1-04__P01
base_head=<wave1 Issue HEAD>
owned_files=<P01 frozen list>
next_action=package_red_green_commit
canonical_agent_id=pending
```

创建返回后绑定 canonical Luna ID。P01 只改 owned files、运行 validation、提交一次并返回。Sol 核验 branch、base ancestry、changed paths、测试结果和 commit 后写唯一 `package_disposition=accepted|rejected`；只有 accepted tip 集成到 Issue HEAD。

target、版本、writer、dispatch intent 或 package 现场无法唯一对应时返回 `restart_recovery_ambiguous` 并保留现场；不得创建第二 target、第二 package 或替代 owner。

## 7. 完成条件

- S00/P01/S01 全部终态，P01 writer terminal 且 package disposition 唯一；
- accepted P01 tip 为最终 Issue HEAD 祖先；无 rejected tip，或 rejected tip 已按契约归档并完成祖先排除；
- 实际 changed paths 与批准设计双向一致，公开 API、FileOperations、AutosaveManager、依赖和 ADR 无差异；
- 测试报告、实施记录、阶段总结与 stage check 绑定当前 HEAD；
- Issue 成果提交洁净，manifest 冻结并经 Sprint merge 验证；
- Sprint 状态更新为 `待验收`，acceptance input-gate 通过；
- 最多尝试一次非强制 Issue/P01 worktree 清理，保留分支；清理失败不回退成功 merge。

本计划冻结 implementation；当前 design 阶段不创建 Issue/package target、不运行实现测试、不修改生产代码。
