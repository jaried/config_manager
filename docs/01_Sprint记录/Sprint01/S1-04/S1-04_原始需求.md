# S1-04 原始需求

## 登记信息

- Issue ID：`S1-04`
- 类型：`BUG`
- 标题：`config.progress` 变化触发配置自动备份
- Sprint：`Sprint01`
- 状态：待办
- 优先级：`P2`
- 估算点数：`2`
- 登记日期：2026-08-25
- 登记来源：当前会话手动调用 `issue-register`

## 治理路由

- `effective_write_directory`：`D:\Tony\Documents\invest2025\project\config_manager`
- `skills_mode`：`global_only`
- `task_intent`：`use_skill_in_project`
- `content_type`：`issue_registration`
- `git_root_real`：`D:\Tony\Documents\invest2025\project\config_manager`
- `target_path`：`D:\Tony\Documents\invest2025\project\config_manager\docs\01_Sprint记录\Sprint01\S1-04\S1-04_原始需求.md`

## 用户原话

### 用户消息 1

```text
$issue-register 日志目录: d:\logs\bakamh\default\logs\20260825\020317\ ，每次config.progress变更都会触发保存，配置已自动备份到 d:\logs\bakamh\default\backup\20260825\020317\config_20260825_020317.yaml
 跳过内部保存触发的文件变化检测，需要跳过因config.progress变化而保存。
```

### 用户消息 2

```text
需要跳过因config.progress变化而保存
```

### 用户消息 3

```text
需要跳过因config.progress变化而自动备份配置和保存config.yaml
```

### 用户消息 4

```text
需要跳过因config.progress变化而自动备份配置，保存config.yaml需要保存
```

### 用户消息 5

```text
1
```

### 用户消息 6

```text
错了，这个不应该登记到本项目，应该登记到 config_manager
```

## 整理结果

- 用户消息 4 确定当前输出边界：因 `config.progress` 变化时跳过配置自动备份，继续保存 `config.yaml`。
- 用户消息 5 只确认了随后撤回的旧目标草案。
- 用户消息 6 将登记目标确定为 `config_manager`；当前正式 Issue 为 `S1-04`。
- 同一触发场景还需跳过内部保存触发的文件变化检测。

## 角色、目标与价值

- 角色：未提供。
- 目标：`config.progress` 变化引发内部保存时，跳过该内部保存触发的文件变化检测和配置自动备份，同时继续保存 `config.yaml`。
- 价值：避免 `config.progress` 变化产生配置自动备份，并保留 `config.yaml` 保存结果。

## BUG 事实

- 触发场景：每次 `config.progress` 变更。
- 实际结果：触发保存，并自动备份配置。
- 原始错误：未提供。
- 日志证据位置：`d:\logs\bakamh\default\logs\20260825\020317\`。
- 自动备份证据位置：`d:\logs\bakamh\default\backup\20260825\020317\config_20260825_020317.yaml`。
- 环境：未提供。

## 数据与结构事实

- 配置项：`config.progress`。
- 需保留的保存目标：`config.yaml`；精确路径未提供。
- 已观察到的备份文件：`config_20260825_020317.yaml`。
- 生产日志、配置文件和备份文件内容未提供。

## 原子边界

- 本 Issue 只登记 `config.progress` 变化引发内部保存时的文件变化检测、配置自动备份和 `config.yaml` 保存结果。
- 父子关系：无。
- 关联 Issue：未提供。
- 显式依赖：未提供。

## 初步验收

- [ ] `config.progress` 变化引发内部保存时，不触发相应的文件变化检测。
- [ ] `config.progress` 变化引发内部保存时，不自动备份配置。
- [ ] `config.progress` 变化引发内部保存时，`config.yaml` 仍保存。

## 歧义与未知

- `config.yaml` 的精确路径未提供；不影响当前期望结果的登记。
- 原始错误信息、运行环境和显式依赖未提供。

## 保真自检

- 草案版本：`v2`
- “未更改原始需求”：`pass`
- “原话原本记录”：`pass`
- 用户消息 1 至用户消息 6 按原始字符、空白、标点、大小写、消息边界与顺序逐字保留，未与整理结果混写。

## 估算依据

- 一个原子 BUG 范围，包含文件变化检测、配置自动备份和 `config.yaml` 保存三个已明确的行为边界；具体根因与处理路径留给后续方案决策。
