# S1-03 遗留事项报告

## 交接结论

- Issue：`S1-03`
- Sprint：`Sprint01`
- 整体评审结论：`changes_required`
- 整体评审报告：`S1-03_整体评审报告.md`
- 实施完成范围：`partial_with_legacy`
- acceptance input-gate：`pass_with_legacy`
- Sprint 状态：`待验收`
- 验收交接：`ready_for_acceptance_with_legacy`
- 新增产品阻塞 finding：无
- 自动整改、回滚、重新派发、复评或历史改写：未执行

整体 Reviewer 冻结的项目版本为 `b912531679c3215f9aba3832560a273a2877db6c`，其中 implementation merge 为 `0e1208ed126526bd004d49aa441925586cc508d0`，Issue 候选为 `d5efae67d716edf34a3b80f58e1fd235988d1153`。需求保真、批准设计符合性、Python 代码质量、依赖声明、静态检查、focused/migrated/affected/E2E 回归、数据库行为边界、版本 lineage、`unapproved_decision_set=[]` 与 `unmerged_paths=[]` 均有通过证据；以下三项既有遗留阻止 clean pass，因此整体结论保持 `changes_required`。

## 遗留事项

### LP-S1-03-001 — 迁移冻结集外旧注释保真断言仍失败

- 严重度：`blocking_clean_pass`
- 评审 profile / unit：`design / D-DO8-06,D-P02`
- 状态：`still_valid`
- 事实：`tests\test_tc0012_007_test_database_switch.py::test_ut11_yaml_comments_and_non_target_fields_are_preserved` 不在 P02 冻结的 11 个迁移归属测试内；该用例在基线通过、当前合并树失败，仍断言 YAML 旧注释文本保真，与批准的 `RC8-01/AC8-06` 规范化语义不一致。
- 证据：冻结基线与 implementation merge 的全量结果对照；M4 全量为 `538 passed / 26 skipped / 6 failed`，其中 1 项明确映射本遗留；原始需求、需求澄清、批准方案、设计、测试报告和唯一遗留问题文件保持同一映射。
- 影响：产生 1 项迁移冻结集外新增全量失败，使设计迁移闭环、受影响回归和全量回归不能取得 clean pass；没有证据表明它破坏 S1-03 产品行为，focused、migrated、affected rebuilt 与 E2E 主链仍通过。
- Owner：`design/test-migration-owner`
- 定位：`S1-03_整体评审报告.md` 中 `LP-S1-03-001`；`S1-03_遗留问题.md#LP-S1-03-001`；`S1-03_测试报告.md`。
- 处理选项：验收时接受并保持 `partial_with_legacy`；登记独立测试迁移 Issue，使断言改为批准的规范化语义；或在后续明确授权范围内修复。
- 约束：不得为消除失败而恢复旧注释保真承诺。

### LP-S1-03-002 — 基线数据库 fixture 的 5 项全量失败仍存在

- 严重度：`blocking_clean_pass`
- 评审 profile / unit：`implementation / I-VERIFY-FULL`
- 状态：`still_valid`
- 事实：全量套件另有 5 个测试节点在当前合并树与冻结基线均因缺少 `database.test_address` fixture 失败；S1-03 不包含数据库连接行为，也未修改该 fixture 或数据库契约。
- 证据：M4 当前合并树全量结果为 `538 passed / 26 skipped / 6 failed`；其中 5 项与冻结基线具有相同失败原因，并已在测试报告、实施记录和唯一遗留问题文件中映射至本遗留；S1-03 实际变更与设计边界均无数据库连接变更。
- 影响：全量命令保持非零退出，implementation profile 无法 clean pass；这是可复现的基线数据库测试隔离/fixture 遗留，未构成 S1-03 新产品回归，也不削弱 focused、migrated、affected rebuilt 或 E2E 结果。
- Owner：`database-fixture/backlog-owner`
- 定位：`S1-03_整体评审报告.md` 中 `LP-S1-03-002`；`S1-03_遗留问题.md#LP-S1-03-002`；`S1-03_测试报告.md`。
- 处理选项：验收时按基线遗留接受并保持 `partial_with_legacy`；登记独立 fixture 修复 Issue；或在后续独立授权范围内修复测试数据。
- 约束：不得通过放宽既有 fail-closed 行为掩盖 fixture 缺失。

### LP-S1-03-003 — IM-12 验收输入证据保持合并前状态

- 严重度：`blocking_clean_pass`
- 评审 profile / unit：`implementation / I-VERIFY-IM12`
- 状态：`still_valid`
- 事实：实施合并与待验收状态均已存在，但验收输入中的 IM-12 证据保持合并前状态，结果为 `unverified`；首次 input-gate 因此失败，按交接契约追加本遗留后复跑为 `pass_with_legacy`。
- 证据：implementation merge、冻结验收输入、实施阶段检查单、实施记录、唯一遗留问题文件及 input-gate 两次运行记录；`unapproved_decision_set=[]`、`unmerged_paths=[]`，但 IM-12 冻结摘要没有被追写为已验证。
- 影响：验收输入不能宣称 clean `pass`，只能以 `pass_with_legacy` 进入整体评审；不影响代码、测试结果、合并 lineage 或产品行为。
- Owner：`implementation-closure-evidence-owner`
- 定位：`S1-03_整体评审报告.md` 中 `LP-S1-03-003`；`S1-03_遗留问题.md#LP-S1-03-003`；`S1-03_实施记录.md#方案实施阶段检查单`。
- 处理选项：验收时接受现有 `pass_with_legacy` 并保留本项；登记独立治理/证据修复 Issue；或未来以新 lineage 补充可审计证据。
- 约束：不得改写既有提交历史或伪造原冻结摘要。

## 非阻塞流程观察

### OBS-S1-03-001 — 主 Issue worktree 清理早于报告交接

- 评审 profile / unit：`overall / O-ISSUE-CLOSE`
- 状态：`nonblocking_observation`
- 事实：原 owner 在整体评审报告与本 Leader 遗留事项报告交接前，提前执行了主 Issue worktree 的一次非强制清理，偏离 `$codex-sprint` 规定的“报告交接后再清理”顺序。
- 证据：主 Issue worktree 已不存在；Issue 分支仍保留在 `S1-03@d5efae67d716edf34a3b80f58e1fd235988d1153`；结果提交、收尾提交和 merge lineage 均可达；三个 package worktree 仍存在且不属于这次主 Issue worktree 清理目标。
- 影响：形成生命周期顺序偏差，但没有造成提交、分支、证据或审计 lineage 丢失；整体评审已经在 Sprint worktree 完成，且没有 remediation 依赖主 Issue worktree，因此不新增 blocking finding。
- Owner：`Sprint-Leader/lifecycle-owner`
- 定位：`S1-03_整体评审报告.md` 中 `OBS-S1-03-001`；任务记录中的 S1-03 acceptance input-gate 与清理终态。
- 处理：在本报告保留观察；当前不重建、不重复清理、不回滚、不改写历史。仅当后续确有操作依赖时，才基于保留分支另行取得授权并显式重建。

## Acceptance input-gate 正式结果

```text
input_gate_result=pass_with_legacy
target_stage=acceptance
input_version=S1-03-implementation-v1
failure_owner=none
failure_reason=none
lineage_action=append_legacy
legacy_ids=[LP-S1-03-001,LP-S1-03-002,LP-S1-03-003]
summary_handoff_status=ready
```

## 验收前必须知悉

1. S1-03 产品主链、focused、migrated、affected rebuilt、E2E、静态检查、依赖和合并 lineage 已具备交接证据。
2. 完整测试结果为 `538 passed / 26 skipped / 6 failed`：1 项由 `LP-S1-03-001` 解释，5 项由 `LP-S1-03-002` 解释。
3. `LP-S1-03-003` 只影响验收输入 clean pass，不影响代码、测试结果或产品行为。
4. `OBS-S1-03-001` 是非阻塞生命周期顺序偏差；主 Issue 分支保留，不重建或重复清理 worktree。
5. 本次 Codex Sprint 不自动整改、回滚、重派或复评；后续 `$acceptance` 必须先向用户逐项报告事实、影响、定位和处理选项，再进行功能验收。

## 2026-08-18 用户授权的后续整改补充

本补充发生在整体 Reviewer 与 Leader 报告冻结之后，不改写当时的 `changes_required`、`538 passed / 26 skipped / 6 failed` 或 acceptance input-gate 历史结论。

- `LP-S1-03-001`：`resolved_post_review`。旧注释文本保真断言已迁移为批准语义下的安全解析与数据等价断言，没有恢复废弃的格式保真承诺。
- `LP-S1-03-002`：`resolved_post_review`。五个 test-mode fixture 已补齐非空、不触网的测试地址 sentinel，没有降低 fail-closed 校验。
- `LP-S1-03-003`：`historical_append_legacy`。原 IM-12 与首次门禁证据继续保留；该项不影响当前产品行为或测试结果。
- 另行修复的真实全量问题：TensorBoard 时间路径断言改为锚定 `tsb_logs` owner；默认路径和直接构造测试均完成临时配置隔离；新增全局生产配置字节/mtime 不变式 guard。
- 当前验证：`D:\anaconda3\envs\base_python3.12\python.exe -m pytest -p no:cacheprovider -q tests` 退出码 0，`544 passed / 26 skipped / 0 failed`；Ruff 和 `git diff --check` 均通过；生产配置 blob 前后不变且无 `.pytest_cache`。
- 当前交接：完整测试套件 clean pass，测试遗留集合为空；S1-03 继续保持 `待验收`，验收时同时展示原冻结报告和本补充证据。
