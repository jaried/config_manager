# S1-05 测试报告

## scope

- 方案：`S1-05-solution-v2-approved`。
- 设计 / 计划：`S1-05-design-v1` / `S1-05-plan-v1`。
- 正式实施候选：`S1-05@892dcd82aeb2858345545eb0e7179a12d0f6f911`。
- 新增测试：
  `tests/01_unit_tests/test_config_manager/test_tc0021_001_progress_autosave_public_chain.py`。
- 验证范围：公开属性入口、keyed autosave callback、主文件 round-trip、保存入口选择、backup 不变、
  autosave intent 清理、关联 autosave/backup/YAML 回归以及完整测试集。
- 生产代码、函数定义与签名、依赖、配置格式、pytest 配置和 ADR 的实际变化均为零。

## environment

- 执行平台：Microsoft Windows Server 2025，`win32`。
- Python：`3.12.10`。
- pytest：`9.1.1`。
- pytest-asyncio：`1.4.0`。
- PyYAML：`6.0.3`。
- psutil：`7.2.2`。
- Ruff：`0.16.5`。
- 实际源码导入：当前 GitHub Actions checkout 的 `src/config_manager/__init__.py`。
- 验证 run：`33667182758`；job：`100371701085`；结论：`success`。
- 验证 checkout：PR 合并树 `3e5809dc57cd0e47aefc7f1ea5b134a84dd0588a`，由临时验证提交
  `8824176601e0575fe6947f625b959728122c0065` 合入正式候选 `892dcd82...` 形成。
- `database_test_isolation_result=not_applicable_zero_connection_scope`。
- 测试不发送邮件，不访问生产配置目录；每次 pytest 使用独立 `--basetemp`。

## commands

### TDD Red

在设计基线 `260b27da0f4dfe5d1b7a92f0b99f979d00f1a6b2` 的独立 worktree 中执行：

```text
python -m pytest \
  tests/01_unit_tests/test_config_manager/test_tc0021_001_progress_autosave_public_chain.py \
  -v --tb=short
```

### 静态规则与 Ruff

```text
python <inline-ast-static-check>
python -m ruff check \
  tests/01_unit_tests/test_config_manager/test_tc0021_001_progress_autosave_public_chain.py
```

### Issue focused

```text
python -m pytest \
  tests/01_unit_tests/test_config_manager/test_tc0021_001_progress_autosave_public_chain.py \
  -v --tb=short --basetemp=<isolated-focused-dir>
```

### Progress focused

```text
python -m pytest \
  tests/01_unit_tests/test_config_manager/test_tc0021_001_progress_autosave_public_chain.py \
  tests/01_unit_tests/test_config_manager/test_progress_autosave.py \
  -v --tb=short --basetemp=<isolated-progress-dir>
```

### Affected

```text
python -m pytest \
  tests/01_unit_tests/test_config_manager/test_tc0021_001_progress_autosave_public_chain.py \
  tests/01_unit_tests/test_config_manager/test_progress_autosave.py \
  tests/01_unit_tests/test_config_manager/test_internal_save_no_reload.py \
  tests/01_unit_tests/test_config_manager/test_tc0017_001_frequent_load_save.py \
  tests/01_unit_tests/test_config_manager/test_tc0012_004_backup_isolation.py \
  tests/01_unit_tests/test_config_manager/test_yaml_codec.py \
  tests/01_unit_tests/test_config_manager/test_tc0011_002_yaml_sequence_persistence.py \
  -v --tb=short --basetemp=<isolated-affected-dir>
```

### Full

```text
python -m pytest tests -v --tb=short --basetemp=<isolated-full-dir>
```

## results

| 门禁 | 收集 / 结果 | 耗时 | 退出码 |
|:---|:---|---:|---:|
| TDD Red | `0 items`；精确测试文件不存在 | `0.00s` | `4`，符合预期 |
| 静态规则 | `154` 行；最长 `91` 字符；`10` 个函数/方法均显式返回 | - | `0` |
| Ruff | `All checks passed!` | - | `0` |
| Issue focused | `1 passed` | `0.11s` | `0` |
| Progress focused | `17 passed` | `0.32s` | `0` |
| Affected | `98 passed, 1 skipped` | `12.42s` | `0` |
| Full | `565 passed, 26 skipped`，共收集 `591` 项 | `193.85s` | `0` |

行为断言全部通过：主配置复读为 `progress: after`；sentinel backup 仍为 `progress: before`；
`save_config_only_calls == 1`；`save_calls == 0`；backup 集合、mtime 与字节内容不变；last backup 未更新；
pending autosave intent 已清空。

## failures

最终验证没有测试或质量失败。

第一轮临时 CI 在完整测试收集阶段出现 `ModuleNotFoundError: psutil`，并因缺少 `pytest-asyncio` 出现 asyncio
配置告警。该轮在完整测试之前的 Red、静态规则、Ruff、Issue focused、Progress focused 和 Affected 均通过。
第二轮只补齐临时测试环境的 `psutil` 与 `pytest-asyncio`，未修改项目依赖声明，完整验证随即通过。

## skips

- Issue focused：`0`。
- Progress focused：`0`。
- Affected：`1` 个既有外部文件变化环境型 skip；新增测试未 skip。
- Full：`26` 个既有平台、权限、可选 multiprocessing start method 或环境型 skip；退出码为 `0`。
- 没有新增 skip、xfail 或断言降级。

## evidence

- 正式测试提交：`892dcd82aeb2858345545eb0e7179a12d0f6f911`。
- TDD 设计基线：`260b27da0f4dfe5d1b7a92f0b99f979d00f1a6b2`。
- 验证工作流提交：`8824176601e0575fe6947f625b959728122c0065`，仅用于运行门禁。
- GitHub Actions：run=`33667182758`，job=`100371701085`，全部步骤结论为 `success`。
- Git 范围：正式实现没有 `.github/workflows/`，实际产品相关差量仅为新增测试文件。
- `unapproved_decision_set=[]`。
- 设计符合性：批准方案、设计、计划、测试实现、运行结果和 changed paths 双向一致。
