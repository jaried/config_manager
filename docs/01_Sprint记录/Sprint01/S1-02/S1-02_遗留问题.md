# S1-02 遗留问题

## LP-001：旧 test_mode 测试夹具未迁移固定数据库测试地址

| 字段 | 内容 |
|---|---|
| 来源阶段 | `implementation` / S04 影响面重建与内联自检 |
| 来源轮次 | 确定性门禁首次发现；内联自检两轮均保持 `changes_required` |
| 类型 | `fact`：当前全量测试的直接失败事实 |
| 规范依据 | `codex-sprint/SKILL.md` 硬边界 1、6；`implementation/SKILL.md` 影响面和防回归检查；冻结设计 `DO-04` / `AC-04` |
| 直接事实 | Issue `.venv` 执行 `pytest tests -v` 收集 505 项，退出码 1：472 passed、26 skipped、7 failed。7 个失败都抛出 `_TestDatabaseConfigurationError: database.test_address must be a non-empty string in test_mode` |
| 失败签名 | 已有测试以 `test_mode=True` 加载存在 `database` 的配置，但夹具未声明非空字符串 `database.test_address`；新契约因此正确快速失败 |
| 严重度 | `high`：项目 full regression 保持非零退出，但 S1-02 聚焦与 affected 集合已通过 |
| 用户影响 | 合并后，已迁移配置使用测试地址并且无效配置不会回退生产地址；但仓库的全量测试仍有 7 个旧夹具未迁移，CI/本地 full suite 会失败 |
| 当前状态 | `open`；本阶段以 `partial_with_legacy` 交接，不得描述为全量回归通过 |
| 推荐 owner | 后续独立 Issue 的 config_manager 测试兼容性 owner |
| 推荐后续动作 | 对下列 5 个旧测试文件的 7 个夹具补充脱敏非空 `database.test_address`，保留既有测试目标和 S1-02 快速失败契约，然后重跑 affected/full |
| 用户可选择 | 待验收时可接受本次部分交付，或明确选择 `LP-001` 调用 `issue-register` 登记后续独立 Issue |

### 影响路径

- `tests/01_unit_tests/test_config_manager/test_config_file_protection.py`
- `tests/01_unit_tests/test_config_manager/test_tc0012_002_first_start_time_preservation.py`
- `tests/01_unit_tests/test_config_manager/test_tc0015_001_yaml_type_validation.py`
- `tests/test_performance_and_stress.py`
- `tests/test_yaml_comments_preservation.py`

### 失败节点

- `TestConfigFileProtection::test_complex_windows_paths_handling`
- `TestFirstStartTimePreservation::test_tc0012_002_004_raw_yaml_format_preservation`
- `test_tc0015_001_006_nested_structure_string_preservation`
- `test_config_access_performance`
- `test_memory_usage_stress`
- `test_yaml_comments_preservation`
- `test_yaml_comments_preservation_with_new_keys`

### 当前阶段边界

这 5 个文件不在 `S1-02-plan-v2` 的冻结 task files/ownership 内，当前 implementation 不修改它们，不放宽生产快速失败契约，也不返回 design-plan 或重建实施计划。

## LP-002：既有生产文件无法通过完整 Ruff review

| 字段 | 内容 |
|---|---|
| 来源阶段 | `implementation` / S04 代码规范与内联自检 |
| 来源轮次 | 代码规范 review 首次发现；内联自检两轮均保持 `changes_required` |
| 类型 | `fact`：候选与实施基线的直接对比事实 |
| 规范依据 | `code-standards-check` Python review；`py-config-logger` 启动锚点约束；`codex-sprint` 实施边界 |
| 直接事实 | 当前候选对 3 个实际 Python changed paths 运行 `ruff check --no-cache` 退出码 1，报告 8 个 E402 和 1 个 E722；在 P01 基线 worktree 对同范围运行得到同样 8 个 E402 和 1 个 E722，本次未新增该签名 |
| 失败签名 | `src/config_manager/config_manager.py` 在 `start_time = datetime.now()` 启动锚点之后保留若干 module import（E402），既有 fallback 方法保留一个裸 `except`（E722） |
| 严重度 | `low`：限定排除 E402/E722 后生产文件通过，两个测试文件完整 Ruff check 通过；但规范 review 聚合结果必须保持 `fail` |
| 用户影响 | 不改变 S1-02 运行时行为；当工程质量门禁对整个生产文件严格执行 Ruff 时仍会非零退出 |
| 影响路径 | `src/config_manager/config_manager.py` |
| 当前状态 | `open`；本阶段以 `partial_with_legacy` 交接，不得把完整 Ruff review 描述为通过 |
| 推荐 owner | 后续独立 Issue 的 config_manager 代码质量 owner，与 py-config-logger 启动锚点规则联合核对 |
| 推荐后续动作 | 在不破坏文件头启动锚点的前提下确定 E402 的项目级规则，独立修复 E722，然后对完整文件重跑 Ruff review |
| 用户可选择 | 待验收时可接受该基线遗留，或明确选择 `LP-002` 调用 `issue-register` 登记后续独立 Issue |

### 当前阶段边界

S1-02 不用大范围格式化、改写既有 fallback 异常语义或添加抑制注释来制造 Ruff 通过；本次只保留完整失败与限定检查的对照证据。
