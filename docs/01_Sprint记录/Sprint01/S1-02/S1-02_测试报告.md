# S1-02 测试报告

## scope

S1-02 固定数据库地址选择、失败闭包、生产源不变、运行时/快照一致、项目文档与全局 Skill 投影，以及相关 test_mode/full regression。测试地址始终为脱敏字符串，不连接或探测任何数据库。

## environment

- Worktree：`D:\Tony\Documents\invest2025\project\config_manager\.worktrees\S1-02`
- Python：Issue `.venv\Scripts\python.exe`，Python 3.12.9；`sys.base_prefix=D:\anaconda3\envs\base_python3.12`
- 源码：editable locator 指向当前 Issue worktree。
- `database_test_isolation_result=not_applicable_zero_connection_scope`：生产代码和测试均无数据库 driver/连接/可达性路径，地址只作为不透明 YAML 字符串。

## commands

| 阶段 | 命令范围 | 退出码 | 摘要 |
|---|---|---:|---|
| red | 新 S1-02 测试文件 | 1 | 22 collected；20 failed/2 passed；失败只指向缺失新行为 |
| plan structure | `validate_implementation_plan.py --max-luna-capacity 1` | 0 | `status=pass units=7 luna_waves=2` |
| E2E | 冻结的 3 个精确 node id | 0 | 3 passed |
| affected | 旧 test_mode 文件 + S1-02 新文件 | 0 | 41 passed |
| Python lint | 两个测试文件 | 0 | `ruff check` pass |
| Python lint | 生产文件，精确排除既有 E402/E722 | 0 | 其余 Ruff 规则 pass |
| Python review | 完整 3 个 Python changed paths，`--no-cache` | 1 | 8 E402 + 1 E722；P01 基线对照签名相同；`LP-002` |
| full | `pytest tests -v` | 1 | 505 collected；472 passed，26 skipped，7 failed |
| Skill | `quick_validate.py` + 固定契约静态检查 | 0 | `Skill is valid!`；固定规则全部可定位 |

## results

E2E 和 affected 完全通过，Skill 结构/内容检查通过。full 直接暴露 5 个未在冻结影响集和 task files 中声明的旧测试文件；影响面重建保持 `violation`，并按 codex-sprint 覆盖规则完整登记为 `LP-001`。完整 Ruff review 的既有失败登记为 `LP-002`。候选总体结果是 `partial_with_legacy`，不是 full/quality pass。

## failures

- red 预期失败：有效地址未切换；无效节点/值未快速失败；仅测试地址未生成活动键。
- full 非终态失败：7 个节点的旧夹具存在 `database` 但缺少 `database.test_address`，因新契约正确抛出 `_TestDatabaseConfigurationError`。根因是冻结影响集漏项，不能用放宽生产契约或未授权临场改测试规避。
- 质量失败：完整 Ruff review 在候选和 P01 基线均报告 8 E402 + 1 E722。本次没有扩大为启动锚点/既有 fallback 重构，也没有添加抑制制造通过；详见 `LP-002`。

## skips

red、E2E 和 affected 无 skip。full 有 26 个项目既有 skip，没有用于规避 S1-02 关联节点。真实数据库连接、认证、权限和端点可达性按批准范围不适用。

## evidence

- P01 package commit：`84a5019caa24cf9b1c1394c64c8fb3f89b57b1d3`。
- P02 package commit：`ba0ffe61af63f72bf8568c17e73e3c5255771963`；全局 Skill commit：`392392955e09f96d61bf3713ffbaad04ddeb7e3c`。
- Red/E2E/affected/full 的工作目录和解释器均为 environment 中的固定 Issue target；直接计数、退出码和不兼容节点见 commands/failures。
- 生产/测试变更路径静态扫描未命中数据库 driver、`create_engine`、socket 或 `.connect(`；`database_test_isolation_result=not_applicable_zero_connection_scope`。
- full 命令产生的 1 个跟踪配置和 15 个跟踪 pyc 已依测试前 clean 证据精确恢复；恢复后 `git status` 只含 E2E 补齐和阶段文档。
- `impact_reconciliation_result=violation_with_legacy`；完整 LP 定位为 `docs/01_Sprint记录/Sprint01/S1-02/S1-02_遗留问题.md`（`LP-001`、`LP-002`），未改写 full/Ruff 的真实失败结果。
