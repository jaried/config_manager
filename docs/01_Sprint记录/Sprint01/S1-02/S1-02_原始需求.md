# S1-02 原始需求

## 登记信息

- Issue ID：`S1-02`
- 类型：`US`
- 标题：支持 `test_mode` 自动切换测试数据库并更新 `py-config-logger`
- Sprint：`Sprint01`
- 状态：待办
- 优先级：`P2`（已确认草案中的“中”）
- 估算点数：`5`
- 登记日期：2026-08-18
- 登记来源：用户直接调用 `issue-register`
- 授权定位：`.codex/task-records/2026/08/18/20260818-issue-register-test-mode-db.md` 的“用户确认”与“用户补充授权”

## 用户原话

### 用户消息 1

```text
$issue-register config，需要支持test_mode=true时，自动切换测试数据库。先配置好测试数据库地址，测试时，自动切换
```

### 用户消息 2

```text
$issue-register config，需要支持test_mode=true时，自动切换测试数据库。先配置好测试数据库地址，测试时，自动切换，并更新 $py-config-logger
```

### 用户消息 3

```text
不拆分issue，作为一个issue
```

## 角色、目标与价值

- 角色：`config_manager` 的使用者。
- 目标：预先配置测试数据库地址，在 `test_mode=true` 时自动切换到测试数据库，并同步更新 `py-config-logger`。
- 价值：测试运行使用明确的测试数据库配置，相关使用规则与配置行为保持一致。

## 已提供事实

- 目标项目为 `config_manager`。
- 触发条件为 `test_mode=true`。
- 测试数据库地址需要预先配置。
- 测试时需要自动切换测试数据库。
- 用户明确要求更新 `py-config-logger`。
- 用户明确要求上述内容作为一个 Issue，不拆分。

## 数据与结构事实

- 已提供的数据项：测试数据库地址。
- 已提供的模式值：`test_mode=true`。
- 测试数据库地址的配置键、配置层级和格式：未提供。
- 未配置测试数据库地址时的行为：未提供。
- `test_mode=false` 时的数据库行为：未提供。
- `py-config-logger` 需要修改的具体规则、示例和参考文档范围：未提供。

## 原子边界

- 本 Issue 共同包含 `config_manager` 的测试数据库自动切换能力与 `py-config-logger` 的对应更新。
- 两项交付共同形成一个方案决策和验收范围，不拆分为并列或子 Issue。
- 父子关系：无。

## 关联与依赖

- 来源 Issue：无。
- 父 Issue：无。
- 子 Issue：无。
- 显式依赖：未提供。

## 初步验收

- [ ] 可以预先配置测试数据库地址。
- [ ] `test_mode=true` 时自动切换到测试数据库。
- [ ] `py-config-logger` 已更新，内容与上述测试数据库切换要求一致。
- [ ] 三项结果作为同一个 Issue 共同验收。

## 保真自检

- 草案版本：`v2`
- “未更改原始需求”：`pass`
- “原话原本记录”：`pass`
- 三条用户消息分别保留消息边界、原始顺序和原始文本。
- 用户确认：已取得，确认回复为 `1`。

## 估算依据

一个统一验收范围包含配置能力、模式切换结果和一个已点名 Skill 的规则更新；具体配置结构与规则修改范围尚未提供。
